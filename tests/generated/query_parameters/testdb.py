# Code generated by iron_sql, DO NOT EDIT.

# fmt: off
# pyright: reportUnusedImport=false
# pyright: reportUnusedParameter=false
# ruff: noqa

import builtins
import datetime
import decimal
import ipaddress
import uuid
from collections.abc import AsyncGenerator
from collections.abc import AsyncIterator
from collections.abc import Sequence
from contextlib import AbstractAsyncContextManager
from contextlib import asynccontextmanager
from contextvars import ContextVar
from dataclasses import dataclass
from enum import StrEnum
from typing import Any
from typing import Literal
from typing import overload
import psycopg
import psycopg.rows
import psycopg.sql
import psycopg.types.json
from iron_sql import runtime
from tests.generated.query_parameters.settings import DSN


TESTDB_POOL = runtime.ConnectionPool(
    DSN,
    name="testdb",
    application_name=None,
)

_testdb_connection = ContextVar[psycopg.AsyncConnection | None](
    "_testdb_connection",
    default=None,
)


@asynccontextmanager
async def testdb_connection() -> AsyncGenerator[psycopg.AsyncConnection]:
    async with TESTDB_POOL.connection_in_context(_testdb_connection) as conn:
        yield conn


@asynccontextmanager
async def testdb_transaction() -> AsyncGenerator[None]:
    async with testdb_connection() as conn, conn.transaction():
        yield


@asynccontextmanager
async def testdb_listen_session(
    channel: builtins.str,
) -> AsyncGenerator[AsyncGenerator[builtins.str]]:
    async with TESTDB_POOL.connection() as conn:
        async with runtime.listen(conn, channel) as payloads:
            yield payloads


async def testdb_notify(
    channel: builtins.str,
    payload: builtins.str = "",
) -> None:
    async with testdb_connection() as conn:
        await runtime.notify(conn, channel, payload)





class Query[T](runtime.Query[T]):
    _connection_factory = builtins.staticmethod(testdb_connection)


class Query_78802d4833fb9f3da0d9bb1d2540299f(Query[None]):
    _locations = ('queries.py:3',)

    _stmt = psycopg.sql.SQL('INSERT INTO users (id, username, is_active)\nVALUES ($1, $2, $3)')

    _row_factory = builtins.staticmethod(psycopg.rows.scalar_row)

    async def execute(self, *, id: uuid.UUID, username: builtins.str, active: builtins.bool) -> None:
        async with self._client_cursor((id, username, active)):
            pass


class Query_2af3c6fe92023709951b72feac0c35b3(Query[uuid.UUID]):
    _locations = ('queries.py:5',)

    _stmt = psycopg.sql.SQL('SELECT id FROM users WHERE id = $1 AND username = $2')

    _row_factory = builtins.staticmethod(runtime.typed_scalar_row(uuid.UUID, not_null=True))

    async def query_all_rows(self, id: uuid.UUID, *, username: builtins.str) -> builtins.list[uuid.UUID]:
        async with self._client_cursor((id, username)) as cur:
            return await cur.fetchall()

    async def query_single_row(self, id: uuid.UUID, *, username: builtins.str) -> uuid.UUID:
        async with self._client_cursor((id, username)) as cur:
            return runtime.get_one_row(await cur.fetchmany(2))

    async def query_optional_row(self, id: uuid.UUID, *, username: builtins.str) -> uuid.UUID | None:
        async with self._client_cursor((id, username)) as cur:
            return runtime.get_one_row_or_none(await cur.fetchmany(2))

    def query_stream(self, id: uuid.UUID, *, username: builtins.str) -> AbstractAsyncContextManager[AsyncIterator[uuid.UUID]]:
        return self._server_cursor((id, username))


class Query_0f879090a746ab867346a355e73f21b4(Query[builtins.int]):
    _locations = ('queries.py:6',)

    _stmt = psycopg.sql.SQL('SELECT count(*) FROM users WHERE username = $1')

    _row_factory = builtins.staticmethod(runtime.typed_scalar_row(builtins.int, not_null=True))

    async def query_all_rows(self, *, u: builtins.str | None) -> builtins.list[builtins.int]:
        async with self._client_cursor((u,)) as cur:
            return await cur.fetchall()

    async def query_single_row(self, *, u: builtins.str | None) -> builtins.int:
        async with self._client_cursor((u,)) as cur:
            return runtime.get_one_row(await cur.fetchmany(2))

    async def query_optional_row(self, *, u: builtins.str | None) -> builtins.int | None:
        async with self._client_cursor((u,)) as cur:
            return runtime.get_one_row_or_none(await cur.fetchmany(2))

    def query_stream(self, *, u: builtins.str | None) -> AbstractAsyncContextManager[AsyncIterator[builtins.int]]:
        return self._server_cursor((u,))


class Query_9f759f5fac5ad7514508a579644d278b(Query[builtins.int]):
    _locations = ('queries.py:7',)

    _stmt = psycopg.sql.SQL('SELECT count(*) FROM users WHERE id = $1 OR id = $2')

    _row_factory = builtins.staticmethod(runtime.typed_scalar_row(builtins.int, not_null=True))

    async def query_all_rows(self, id: uuid.UUID, id2: uuid.UUID) -> builtins.list[builtins.int]:
        async with self._client_cursor((id, id2)) as cur:
            return await cur.fetchall()

    async def query_single_row(self, id: uuid.UUID, id2: uuid.UUID) -> builtins.int:
        async with self._client_cursor((id, id2)) as cur:
            return runtime.get_one_row(await cur.fetchmany(2))

    async def query_optional_row(self, id: uuid.UUID, id2: uuid.UUID) -> builtins.int | None:
        async with self._client_cursor((id, id2)) as cur:
            return runtime.get_one_row_or_none(await cur.fetchmany(2))

    def query_stream(self, id: uuid.UUID, id2: uuid.UUID) -> AbstractAsyncContextManager[AsyncIterator[builtins.int]]:
        return self._server_cursor((id, id2))


_QUERIES: builtins.dict[builtins.str, builtins.type[Query[Any]]] = {
    'INSERT INTO users (id, username, is_active)\nVALUES (@id, @username, @active)': Query_78802d4833fb9f3da0d9bb1d2540299f,
    'SELECT id FROM users WHERE id = $1 AND username = @username': Query_2af3c6fe92023709951b72feac0c35b3,
    'SELECT count(*) FROM users WHERE username = @u?': Query_0f879090a746ab867346a355e73f21b4,
    'SELECT count(*) FROM users WHERE id = $1 OR id = $2': Query_9f759f5fac5ad7514508a579644d278b
}


@overload
def testdb_sql(sql: Literal['INSERT INTO users (id, username, is_active)\nVALUES (@id, @username, @active)']) -> Query_78802d4833fb9f3da0d9bb1d2540299f: ...
@overload
def testdb_sql(sql: Literal['SELECT id FROM users WHERE id = $1 AND username = @username']) -> Query_2af3c6fe92023709951b72feac0c35b3: ...
@overload
def testdb_sql(sql: Literal['SELECT count(*) FROM users WHERE username = @u?']) -> Query_0f879090a746ab867346a355e73f21b4: ...
@overload
def testdb_sql(sql: Literal['SELECT count(*) FROM users WHERE id = $1 OR id = $2']) -> Query_9f759f5fac5ad7514508a579644d278b: ...
@overload
def testdb_sql(sql: builtins.str) -> Query[Any]: ...


def testdb_sql(
    sql: builtins.str,
    row_type: builtins.str | None = None,
) -> Query[Any]:
    if sql in _QUERIES:
        return _QUERIES[sql]()
    msg = f"Unknown statement: {sql!r}"
    raise builtins.KeyError(msg)
